"""An autonomous agent that manages a LaLiga Fantasy squad.

Perceives the league, plans signings against known buyout windows, asks for
approval before the moment arrives, and executes unattended within a policy the
manager owns.

See README.md for the architecture and docs/ for the decisions behind it.
"""

__version__ = "0.1.0"
